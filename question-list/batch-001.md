# Question list: batch 001

Questions 1–500. This is a question-only authoring list, not an answered dataset.
Questions 1–30 retain the list already shared in the conversation; 31–500 extend it.
These entries were written individually. They were not produced by substituting
topic names into a shared question template. This list is separate from legacy
`data/*.json`; overlap with legacy coverage is reviewed and disclosed separately.

1. In C#, what happens when a CancellationTokenSource is disposed while an operation still holds its token?
2. How would you prevent concurrent requests from refreshing the same expired cache entry in an ASP.NET Core application?
3. When can returning IAsyncEnumerable<T> from an API leave a database connection open longer than expected?
4. How would you detect a scoped service accidentally retained by a singleton?
5. Why can an EF Core query return stale entity values even after another process updates the database?
6. How would you implement optimistic concurrency when two users edit different fields of the same record?
7. What can go wrong if a retry policy repeats a database operation after the commit succeeded but its acknowledgement was lost?
8. How would you paginate records ordered by a timestamp when several records have identical timestamps?
9. When can a filtered SQL index be unsuitable for a parameterized query?
10. How would you investigate a stored procedure that is fast for most customers but consistently slow for one large customer?
11. How would you enforce uniqueness across records that use soft deletion?
12. How would you add a required column to a large production table without breaking older application instances?
13. What happens when a Service Bus consumer completes a message after its lock has expired?
14. How would you make a scheduled Azure Function safe when two invocations overlap?
15. How would you rotate an application's credentials while existing instances continue serving requests?
16. How would you prevent a delayed search response from replacing newer results in React?
17. When should React component state reset after a route parameter changes, and how would you implement that deliberately?
18. How would you preserve unsaved form edits when fresh server data arrives?
19. How would you test whether an Angular component releases subscriptions when repeatedly mounted and destroyed?
20. How would you prevent duplicate submissions when an Angular form is submitted through both a button and the Enter key?
21. How would you diagnose an API that works in a browser tab but fails when called from another site's frontend?
22. How should an API distinguish a missing JSON property from a property explicitly set to null?
23. How would you revoke access immediately when an already-issued access token remains valid?
24. How would you prevent an authenticated user from downloading another tenant's file by changing its identifier?
25. How would you design an idempotency-key policy when a client retries the same key with a different request body?
26. How would you test a timeout path without making the test wait for the real timeout duration?
27. How would you verify that a database migration preserves data meaning, rather than merely preserving row counts?
28. How would you detect whether a mocked dependency allows behavior the real dependency rejects?
29. How would you distinguish missing retrieval evidence from faulty answer generation in a RAG application?
30. How would you stop an AI agent from repeating a tool action after an uncertain network failure?
31. A C# record contains a mutable List<string>; what equality and mutation surprises can occur when the record represents an application setting?
32. A readonly struct exposes an array field through a property; which parts of its state can a caller still change?
33. A dictionary key uses a culture-sensitive string comparison, but identifiers are generated by another service; how would you define a stable comparison contract?
34. A C# event publisher outlives its subscribers; how would you prove whether event subscriptions are retaining abandoned screens?
35. A library exposes both synchronous and asynchronous disposal; how should a consumer choose the correct path for a resource that flushes remotely?
36. An ASP.NET Core request finishes before a fire-and-forget operation uses HttpContext; how would you redesign the operation's inputs and ownership?
37. A BackgroundService catches every exception and continues; what evidence would tell you that restarting the service is safer than continuing its loop?
38. A hosted worker receives cancellation halfway through publishing a batch; what information must survive shutdown to resume without losing work?
39. A custom middleware writes part of a response before the downstream handler throws; what error behavior remains possible?
40. A reverse proxy supplies forwarding headers that clients can also submit; how should ASP.NET Core decide which address information to trust?
41. Two authentication schemes are registered, but one endpoint must accept only service credentials; how would you prevent fallback to a user cookie?
42. A model binder accepts an empty string for a value object that requires a nonempty code; at which boundaries should validation run?
43. A JSON converter calls the same serializer with unchanged options for its own target type; how would you diagnose and remove recursive conversion?
44. A decimal field becomes a string in a partner API; how would you evolve the .NET contract without silently accepting malformed amounts?
45. A minimal API returns different concrete results from different branches; how would you keep its documented response types accurate?
46. A .NET endpoint streams a large download; how should client disconnects affect file reads, logging, and completion metrics?
47. Several tasks share one EF Core DbContext because each method awaits its own query; why might the combined execution still be unsafe?
48. An EF Core projection includes an entity instance inside an anonymous result; how would you check whether that entity is being tracked?
49. A detached entity graph marks every child as modified; how would you avoid overwriting fields the client never edited?
50. A navigation property loads successfully during debugging but fails after the request scope ends; what hidden dependency would you investigate?
51. A global tenant filter is enabled, but an administrative report must span tenants; how would you make the bypass explicit and auditable?
52. An EF Core interceptor stamps audit fields, but a bulk update skips them; how would you preserve the audit invariant across write paths?
53. A LINQ query works with an in-memory collection but cannot translate to SQL; how would you choose between rewriting and client-side evaluation?
54. A SaveChanges call succeeds for one aggregate and fails for another; how would you establish the actual transaction boundary?
55. A database-generated identifier is needed before an outbox event is stored; how would you arrange the write without publishing an incomplete event?
56. A C# iterator yields pooled buffers; what ownership rule prevents a caller from reading a buffer after it has been returned to the pool?
57. A Memory<T> slice is captured by a long-running operation; how would you account for the lifetime of its backing storage?
58. A library returns ValueTask<T>; what constraints should callers understand before caching or awaiting the result more than once?
59. A Parallel.ForEachAsync operation calls an external API with a shared rate limit; where would you enforce both concurrency and request-rate bounds?
60. A SemaphoreSlim is released in finally even when acquisition was cancelled; what failure does this introduce, and how would you prevent it?
61. A Task.WhenAll call fails after several tasks throw; how would you inspect every failure while preserving the caller's expected exception behavior?
62. A linked cancellation token combines request timeout and host shutdown; how would you report the cause without incorrectly treating shutdown as a user error?
63. A retry delay receives no cancellation token; how can this affect deployment shutdown time even if the network operation is cancellable?
64. A custom synchronization context deadlocks a library method that blocks on Task.Result; what would you change in the library's public API?
65. A Channel<T> writer finishes with an exception; how should consumers distinguish normal completion, producer failure, and their own cancellation?
66. A bounded channel uses a drop policy for telemetry; what counters would prove how much data was intentionally discarded?
67. An ArrayPool<byte> buffer contains a previous request's bytes; which conditions make returning or exposing that buffer a confidentiality problem?
68. A parser accepts ReadOnlySpan<char>, but its result must survive the call; what data must it copy or otherwise own?
69. A logging scope contains a mutable dictionary later changed by another task; how could that distort recorded request context?
70. A custom exception stores a full request object; what memory and disclosure risks would you inspect before retaining it in a failure queue?
71. A singleton options consumer must observe configuration changes; how would you choose an options interface and test its update behavior?
72. A configuration reload changes two related settings separately; how would you stop a worker from observing an invalid combination?
73. A .NET health endpoint succeeds while every database connection is exhausted; how would you separate process liveness from service readiness?
74. A container stops accepting traffic but continues processing old requests; how would you coordinate readiness, draining, and host shutdown deadlines?
75. A .NET application allocates many large response arrays; which measurements would distinguish retained objects from garbage-collection pressure?
76. A compiled regular expression processes attacker-controlled text; how would you bound pathological matching time and input size?
77. A reflection-based plugin works locally but fails after trimming; how would you identify the metadata the published build must preserve?
78. An assembly is loaded into a collectible context but never unloads; what references would you inspect before blaming the runtime?
79. An API maps all OperationCanceledException instances to a timeout response; when could that hide a different cancellation decision?
80. A binary protocol reader assumes a single Stream.Read fills its buffer; how would you implement and test exact-length reads and truncated input?
81. An inventory UPDATE checks available quantity in its WHERE clause; how should the application interpret zero affected rows under concurrency?
82. Two SQL sessions check that a booking slot is free before inserting; what database constraint or locking strategy prevents both from succeeding?
83. A SQL query ranks employees by salary within departments; how would you distinguish tie-preserving ranking from selecting exactly three employees?
84. A running balance query contains multiple transactions at the same timestamp; what ordering and window frame make the calculation deterministic?
85. A LEFT JOIN report loses customers after a child-table filter is added; how would you locate the change in its null-preserving behavior?
86. A NOT IN query returns no rows when its subquery contains NULL; how would you express the intended exclusion reliably?
87. A count of orders doubles after joining order lines and payments; how would you restore the intended aggregation grain?
88. A recursive hierarchy query encounters a cycle introduced by bad data; how would you detect it and bound traversal?
89. A SQL report must include calendar dates with no sales; how would you construct missing rows without inventing sales records?
90. A top-selling-products query must include products tied at the cutoff; how would you define its tie policy and resulting page size?
91. An index begins with tenant_id, but a new query filters only by status; how would you evaluate whether the index remains useful?
92. A query compares a numeric column to a string parameter; what would you inspect for implicit conversion and index-access changes?
93. A date predicate wraps the indexed timestamp in a function; how could you express the same interval as a search-friendly range?
94. A text search needs an arbitrary substring rather than a prefix; what indexing alternatives fit that requirement and what do they cost?
95. A covering index includes a large description column; how would you measure whether avoided lookups justify the extra storage and write work?
96. A query has accurate statistics overall but a rare parameter produces a bad plan; how would you demonstrate data skew within that parameter's group?
97. An execution plan estimates one row and processes a million; which operators would you inspect to explain the downstream cost?
98. A report uses OFFSET pagination while new records arrive; how would you demonstrate skipped or repeated results and evaluate a cursor alternative?
99. A cursor-based API lets users change sort direction between requests; how should the cursor encode the ordering contract?
100. A table stores timestamps in UTC but users filter by local calendar days; how would you build correct ranges around daylight-saving transitions?
101. A nullable composite key must be unique according to business rules; how would you verify the database engine's NULL semantics before choosing a constraint?
102. A migration backfills millions of rows; how would you choose batch boundaries that can restart after a process failure?
103. An online index operation competes with peak traffic; which blocking and resource observations would trigger pausing or rescheduling it?
104. A foreign key is added to historical data with orphan rows; how would you stage cleanup and enforcement without hiding the bad records?
105. A column rename must support old and new application versions simultaneously; what sequence avoids a deployment-order dependency?
106. A money column changes scale; how would you detect values that would round, overflow, or violate reconciliation totals?
107. A deadlock graph shows two procedures touching tables in opposite orders; how would you change access order and validate the impact?
108. A transaction waits on a lock while its caller has already timed out; how would you determine whether the database work is still running?
109. A snapshot-isolated transaction reads two related rows and updates only one; how would you check for a write-skew anomaly?
110. A serializable transaction is retried by the application; which operations must remain inside the retried unit to preserve correctness?
111. A SQL job moves old records to an archive; how would you prove that a crash cannot leave a record missing from both locations?
112. A nightly purge deletes a large share of a table; how would you compare batched deletion, partition removal, and an archival requirement?
113. A database replica serves a read immediately after a primary write; how would you offer read-your-writes behavior despite replication lag?
114. A failover promotes a replica after an acknowledged write; how would you verify whether that write met the required durability guarantee?
115. A connection pool resets session state incompletely; which tenant or transaction settings could leak into the next request?
116. A stored procedure builds dynamic ORDER BY text from input; how would you allow supported sort fields without accepting arbitrary SQL?
117. A SQL export contains embedded newlines and quotes; how would you test that a CSV round trip preserves each field exactly?
118. A bulk import must reject malformed rows but retain valid ones; how would you design staging, error reporting, and replay boundaries?
119. A deduplication task finds records with equal emails but conflicting customer IDs; what evidence is needed before merging them?
120. A materialized report is refreshed while users read it; how would you prevent users from seeing an incomplete refresh?
121. A stored procedure creates temporary tables with different sizes on different calls; how would you evaluate plan reuse and compilation overhead?
122. A query becomes slower after removing an apparently unused index; what dependencies outside direct index seeks would you investigate?
123. A table has many updates to a small set of rows; how would you distinguish row contention from page or index contention?
124. A dashboard needs approximate distinct-user counts at massive scale; how would you explain accuracy bounds and unsuitable use cases?
125. A database backup succeeds but restore exceeds the recovery objective; which restore steps would you time and improve?
126. A restore must recover to just before an accidental DELETE; what backup-chain evidence is required before promising that recovery point?
127. A JSON column contains several schema versions; how would you query and validate required fields without assuming every row has the newest shape?
128. A SQL API allows clients to request any combination of optional filters; how would you avoid one universal query plan becoming pathological?
129. A sequence-generated identifier has gaps after rollback; how would you explain why gapless business numbering requires a different design?
130. A reporting query uses dirty reads to avoid blocking; which inconsistent results could invalidate financial or inventory totals?
131. An Azure managed identity is recreated with its resource; how would you find and repair role assignments tied to the old principal?
132. An Azure application can obtain a token but receives authorization failures from storage; how would you separate token audience from role-assignment problems?
133. A managed identity has control-plane rights but cannot read blob contents; what distinction would you verify in its access configuration?
134. A user-assigned identity is attached to several workloads; how would you evaluate whether shared permissions create an excessive failure scope?
135. A service principal secret expires during a release freeze; how would you implement rotation that does not depend on redeploying application code?
136. An Entra access token validates for one API but is rejected by another; which claims and registration settings would you compare?
137. A multi-tenant Entra application receives a token from an unexpected tenant; how should tenant acceptance be decided beyond validating its signature?
138. A workload moves from a developer account to managed identity; how would you prove production is not silently falling back to another credential source?
139. An APIM policy rewrites a backend URL; how would you preserve query parameters and prevent unintended destination selection?
140. APIM caches a response containing tenant-specific data; which cache-key dimensions and authorization checks must be verified?
141. An APIM rate limit protects an expensive backend operation; how would you choose an identity key that clients cannot freely rotate?
142. A client disconnects during an APIM-to-backend request; how would you determine whether backend processing continued and caused side effects?
143. An APIM deployment changes policy fragments used by multiple APIs; how would you identify every affected operation before rollout?
144. A Service Bus message repeatedly fails deserialization; when should it be dead-lettered instead of retried, and what diagnostic metadata should accompany it?
145. A Service Bus session processes ordered events but a handler runs them concurrently; how would you preserve the required business ordering?
146. A queue consumer receives the same business command with different message IDs; where should deduplication live?
147. A Service Bus dead-letter queue is replayed after a bug fix; how would you prevent obsolete commands from damaging current state?
148. A topic subscription's filter changes while messages are flowing; how would you determine which events were missed or routed differently?
149. A Service Bus producer batches messages near a size limit; how would you handle one oversized message without dropping the rest?
150. A queue's age rises while its message count stays stable; what workload changes or poison-message behavior would you investigate?
151. An Azure Function's HTTP caller times out while execution continues; how would you expose a durable operation status to the caller?
152. A queue-triggered Function scales faster than its database can handle; where would you constrain demand and observe downstream saturation?
153. An Azure Function instance restarts between two external side effects; how would you establish which steps need replay or compensation?
154. A Durable Functions orchestrator reads the current wall clock directly; what replay behavior would you investigate before shipping it?
155. A Durable Functions activity completes but its result acknowledgement is lost; how would you design the activity's external writes?
156. A timer-triggered Function misses a scheduled run during an outage; how would you define catch-up behavior and prevent duplicate period processing?
157. A Logic App workflow receives an event after its business deadline; how would you distinguish technical delivery success from an expired business action?
158. A Logic App connector retries a failed request automatically; how would you discover its retry behavior and avoid conflicting application retries?
159. A blob upload is interrupted before completion; how would clients identify whether the intended object is available and valid?
160. Several writers update blob metadata used as a processing checkpoint; how would you protect against lost updates?
161. A blob lease holder crashes; how would another worker safely recover ownership without assuming the old worker has stopped executing?
162. A SAS URL leaks through analytics logs; how would you bound its access and investigate whether it was used?
163. A storage account firewall is tightened; how would you test application access from its actual production network path?
164. A private endpoint exists but the app still resolves the public storage address; what DNS resolution chain would you inspect?
165. A blob lifecycle rule deletes old files that remain referenced by reports; how would you coordinate retention with application-level references?
166. A Cosmos DB partition becomes hot while account-wide throughput is sufficient; how would you identify and redesign the partition-key distribution?
167. A Cosmos DB query fans out across partitions to serve one customer; what data-model or query changes could make it targeted?
168. A Cosmos DB client receives throttling responses during a short burst; how would you honor retry guidance without exceeding the caller's deadline?
169. A Cosmos DB transaction needs to modify records with different partition keys; what alternatives preserve the actual business invariant?
170. A Cosmos DB change-feed consumer checkpoints before committing its projection; what recovery failure can occur?
171. A cache hosted in Azure loses its contents during failover; how would you prevent a database overload as the application warms it again?
172. A cache key contains only a user ID while the application now supports several tenants; how would you migrate keys without mixing user data?
173. An App Service slot swap promotes an app with incompatible background workers; how would you control which slot runs scheduled work?
174. An App Service is healthy from inside its container but unavailable through the public endpoint; which routing and access layers would you test?
175. An AKS pod passes readiness while it cannot reach a required private service; how would you decide whether that dependency belongs in readiness?
176. An AKS rolling update stalls because of disruption and capacity constraints; how would you identify the constraint without disabling protections blindly?
177. An Azure Key Vault secret changes but the application still uses the previous value; where would you inspect caching and refresh behavior?
178. Azure Monitor alert volume spikes after adding a high-cardinality dimension; how would you preserve useful detail while controlling telemetry cost?
179. A regional Azure outage affects a dependency that was assumed to be global; how would you map the real regional failure boundaries?
180. Infrastructure deployment succeeds but data-plane operations fail; what post-deployment checks would you add before declaring the Azure environment ready?
181. A React list uses array indexes as keys and supports drag reordering; how would you reproduce state moving to the wrong row?
182. A React editor opens two records with the same component instance; how would you prevent one record's validation state leaking into the other?
183. A child component copies a prop into local state on mount; what happens when the parent later supplies a different value?
184. A React component has several Booleans representing mutually exclusive request states; how would you prevent impossible combinations?
185. A state update depends on the previous value and several updates occur in one handler; how would you avoid dropping increments?
186. A React interval callback keeps seeing an old filter value; how would you fix the captured-state problem without creating unnecessary intervals?
187. An effect starts a subscription whose setup returns asynchronously; how would you handle unmounting before setup completes?
188. A React effect updates a dependency every time it runs; how would you identify and remove the feedback loop?
189. An effect sends an analytics event twice during development checks; how would you determine whether the effect has an unsafe lifecycle assumption?
190. A React parent creates a new object prop on each render; when would memoizing the child fail to reduce work?
191. A memoized React callback captures stale authorization state; how would you balance stable identity with correct dependencies?
192. A React context contains both frequently changing cursor position and rarely changing permissions; how would you reduce unrelated consumer updates?
193. A React component reads an external mutable store during rendering; how would you avoid inconsistent snapshots across components?
194. A derived total is stored independently from its source items; how could a React update leave them inconsistent?
195. A React form needs to undo the last edit across several fields; what state representation makes undo precise?
196. A server rejects an optimistic React update after the user has made another edit; how should rollback avoid erasing newer work?
197. A React mutation succeeds but refreshing its query fails; how should the interface represent the mixed outcome?
198. A file upload component must survive navigation to another route; where should its state and cancellation ownership live?
199. A React search field must update immediately while an expensive result view lags; how would you separate input responsiveness from rendering work?
200. A React table renders thousands of rows with expandable content; what state must remain stable when rows are virtualized?
201. A virtualized React list loses keyboard focus when an item scrolls out of view; how would you design accessible focus behavior?
202. A React modal is rendered through a portal; how would you manage focus return and interaction with content behind it?
203. A modal closes when any click bubbles to an overlay; how would you prevent clicks inside the dialog from dismissing it unintentionally?
204. A React menu supports keyboard navigation; how would you represent the active item independently from the selected value?
205. A server-rendered React page formats dates in different timezones on server and client; how would you avoid a hydration mismatch?
206. A React component reads localStorage during its initial server render; how would you structure browser-only initialization?
207. A React page uses random IDs during rendering for label associations; what requirements should a replacement ID strategy meet?
208. A Suspense fallback replaces a large page region during a minor interaction; how would you choose a boundary that preserves useful context?
209. A React error boundary catches a render failure but not a rejected click-handler promise; where should that failure be handled?
210. An error boundary immediately fails again after the user clicks retry; what state must be reset or corrected before retrying?
211. A React route loader fetches data that a component fetches again on mount; how would you choose one owner for initial loading?
212. A query cache keeps data from a previous login after account switching; how would you scope and clear cached React application state?
213. A React component uses a URL parameter as both a filter and a local input value; how would you avoid update loops between them?
214. A user presses Back after changing several filters; which React state should be restored from the URL rather than memory?
215. A React application must warn about unsaved work when closing a tab and when navigating internally; why are these separate integration points?
216. A React component exposes an imperative focus method through a ref; what should happen when its input is conditionally absent?
217. A reusable React input receives undefined first and a string later; how would you prevent a controlled/uncontrolled transition?
218. A React number input temporarily contains a minus sign while typing; how would you preserve editing state without accepting an invalid final number?
219. A React date picker must distinguish an empty value from an invalid typed date; what state and error contract would you expose?
220. A component library passes unknown styling props to DOM elements; how would you prevent attribute leakage while retaining composition?
221. A React application loads a third-party widget that mutates its container; what ownership boundary prevents reconciliation conflicts?
222. A React chart updates on every pointer event; how would you measure whether event handling, rendering, or drawing is the bottleneck?
223. A React component subscribes to a ResizeObserver; how would you prevent an observed size change from causing an endless resize cycle?
224. A React image gallery shifts after images load; how would you reserve layout space without knowing every image's exact displayed width?
225. A React app supports right-to-left layouts; which keyboard and spatial assumptions in a custom carousel would you review?
226. A localization update changes labels while a React form is open; how would you keep stable field identity independent from translated text?
227. A React notification disappears before a screen reader can announce it; how would you design announcement and dismissal timing?
228. A React component test passes with mocked timers but fails in the browser; how would you determine whether the test changed event ordering?
229. A React development build feels slow but a production profile does not; which measurements should guide an optimization decision?
230. A React dependency upgrade changes how an effect is scheduled; how would you test observable behavior without asserting internal render counts?
231. An Angular child receives a mutable object whose nested field changes without replacing the object; how would you investigate an unchanged view?
232. An Angular computed signal derives a list from another signal; when should the derivation remain computed rather than being copied into writable state?
233. An Angular effect writes to state that it also reads; how would you remove an unintended reactive feedback cycle?
234. An Angular service exposes a writable signal directly to consumers; what invariant could be lost, and how would you limit mutation authority?
235. A value arrives through an RxJS stream but a component uses signals; where would you establish conversion and lifecycle ownership?
236. An Angular template calls an expensive method repeatedly; how would you determine whether a computed value, pipe, or data-model change fits best?
237. A pure Angular pipe receives a collection mutated in place; how would you explain why its displayed output may remain unchanged?
238. An Angular list tracks rows by position while items are inserted at the front; which component state can attach to the wrong item?
239. A dynamically created Angular component needs a tenant-specific provider; how would you choose and dispose its injector?
240. An Angular root-provided service stores page-specific form data; how could navigation accidentally preserve data longer than intended?
241. Two lazy Angular features expect separate service instances; how would you verify the provider scope actually gives them isolation?
242. An Angular interceptor adds credentials to every request, including third-party URLs; how would you restrict its destination policy?
243. Two failed Angular HTTP requests trigger token refresh simultaneously; how would you coordinate one refresh and replay the requests safely?
244. An Angular interceptor retries a POST after a connection failure; what server contract is needed before the retry is safe?
245. An Angular HTTP request is cancelled by unsubscribing; how would you avoid assuming that cancellation rolled back server-side work?
246. An Angular autocomplete uses mergeMap and displays out-of-order results; what flattening behavior matches replacement searches?
247. A save button must ignore further clicks until its request finishes; how would you choose an RxJS operator for that interaction?
248. A sequence of dependent updates must run in order; how would you queue them without allowing unbounded pending work?
249. A shared Angular observable caches an error permanently; what reset behavior should a recoverable data service provide?
250. An Angular component manually subscribes to a stream already bound with an async pipe; how would you identify duplicate side effects?
251. A route resolver fails before an Angular page activates; where should users see a recoverable error and retry action?
252. An Angular guard fetches permissions, but those permissions change after activation; what must enforce ongoing authorization?
253. An Angular form blocks navigation only when dirty; how would you distinguish harmless formatting changes from unsaved business edits?
254. A reused Angular route changes its ID parameter without recreating the component; how should data loading react to the new identifier?
255. An Angular route parameter controls a database lookup; where should parsing and rejection happen before the request is sent?
256. A custom Angular ControlValueAccessor emits a change while writeValue is called; what update loop can result?
257. A custom Angular control supports disabled state but still emits edits from its inner input; what contract is being violated?
258. An Angular form includes a disabled calculated field that must be submitted; how would you choose the correct value extraction behavior?
259. A form-level Angular validator depends on two child fields; how should it avoid overwriting unrelated child validation errors?
260. An asynchronous Angular validator checks a username that changes during the request; how would you prevent a stale validation result?
261. A form array removes a middle row while validation is pending; how would you ensure errors remain attached to the correct logical row?
262. An Angular form resets after successful submission, but its initial values are loaded asynchronously; what baseline should reset restore?
263. An Angular component projects several content regions; how would you define optional slots without duplicating the consumer's markup?
264. A directive adds a global document listener; how would you verify cleanup when its host element disappears?
265. An Angular directive changes styles while another directive controls the same property; how would you establish predictable ownership?
266. An Angular component wraps a browser-only library and is rendered on the server; how would you isolate initialization from server execution?
267. An Angular server-rendered page repeats its initial HTTP call during hydration; what data-transfer strategy would you evaluate?
268. An Angular app uses browser storage for theme selection; how would you avoid showing the wrong theme before hydration completes?
269. An Angular application renders untrusted rich text; how would you preserve allowed formatting without bypassing sanitization indiscriminately?
270. A component uses a sanitization bypass because a URL was blocked; what evidence is required before treating that URL as trusted?
271. An Angular dialog opens from a table row that later disappears; where should keyboard focus return when the dialog closes?
272. A custom Angular tabs component changes visual state but omits keyboard behavior; what interactions should its accessibility contract include?
273. An Angular table receives frequent websocket updates; how would you coalesce rendering work while preserving the latest data?
274. An Angular service stores each websocket event forever for replay; how would you bound memory and define replay retention?
275. A websocket reconnect delivers events that overlap with an HTTP refresh; how would you deduplicate and order the combined Angular state updates?
276. An Angular feature flag changes while a route is active; how would you handle state and navigation if the current feature becomes unavailable?
277. An Angular test uses a mocked child component and misses an input contract break; which integration test would expose the mismatch?
278. An Angular test passes only after calling detectChanges several times; how would you find the real asynchronous boundary it is hiding?
279. An Angular production bundle contains an unexpected large dependency; how would you trace the import path and verify whether it can be deferred?
280. An Angular upgrade removes a deprecated API used by a shared library; how would you stage compatibility across dependent applications?
281. A JavaScript callback is passed directly as a method reference and loses its receiver; how would you preserve the intended this value?
282. An arrow function is used as an object method that needs a dynamic receiver; why can its lexical this produce the wrong result?
283. A JavaScript object getter performs network-related state changes when logging inspects it; why is that a problematic object contract?
284. A Proxy forwards ordinary property reads but breaks a built-in object's method calls; what receiver assumptions would you inspect?
285. A property exists on an object's prototype but not on the object itself; when does that distinction matter for validating parsed input?
286. A JavaScript merge operation accepts keys such as __proto__; how would you test whether the merge changes object behavior unexpectedly?
287. A JavaScript Map uses object keys reconstructed from JSON; why does an apparently identical key fail to retrieve a value?
288. A WeakMap is chosen for a cache that must list all active keys; what requirement conflicts with that collection's interface?
289. A JavaScript Set deduplicates objects containing equal IDs; how would you implement the intended ID-based uniqueness?
290. A sparse JavaScript array is processed by several iteration methods; how would you determine which methods visit holes?
291. A JavaScript array sort comparator returns only true or false; what ordering requirements does it fail to express?
292. A large array is spread into a function call; what runtime limit could make a mathematically simple operation fail?
293. A JavaScript reduce operation receives an empty array without an initial value; how should the API define the empty-input result?
294. A numeric ID exceeds the safe integer range in JavaScript; how would you preserve it across JSON, storage, and comparisons?
295. A BigInt reaches JSON.stringify; how would you define an interoperable representation rather than relying on implicit conversion?
296. A JavaScript price calculation accumulates rounding differences; where should a decimal representation or integer minor-unit policy be introduced?
297. A numeric input contains whitespace, an empty string, or trailing letters; how would you choose parsing and validation rules deliberately?
298. A JavaScript date-only string is displayed as the previous day in one timezone; what distinction between a date and an instant was lost?
299. A browser timer drives an elapsed-time display while the tab is backgrounded; how would you calculate time without assuming punctual callbacks?
300. A recursive chain of microtasks prevents rendering; how would you yield control while preserving work ordering?
301. A promise executor throws before registering a callback; how does that failure reach consumers compared with a later timer callback throwing?
302. An async function returns a promise from another function without awaiting it inside try; when could its catch fail to handle rejection?
303. Promise.race returns after a timeout, but the original fetch continues; how would you connect timeout selection to actual cancellation?
304. Promise.all rejects after one request fails; how would you collect partial results and decide whether remaining work should continue?
305. A promise's finally callback throws; what should a caller expect as the final outcome?
306. An AbortController is reused after being aborted; what happens to later operations that receive its signal?
307. A JavaScript retry loop retries parse failures as well as network failures; how would you classify retryable errors?
308. An async iterator consumer stops early; what cleanup contract should the iterator provide for an open stream?
309. A browser event listener is registered with an anonymous callback each render; how would you remove exactly the registered listener?
310. A passive touch listener calls preventDefault; how would you investigate why scrolling is not prevented?
311. A click handler awaits a promise before opening a new window; what browser interaction constraint could change the outcome?
312. A web worker receives a transferred ArrayBuffer; what operations remain valid on the sender's original buffer?
313. A worker protocol must evolve while an old service worker controls a page; how would you version request and response messages?
314. A BroadcastChannel carries logout events across tabs; how would you handle a tab opened after the broadcast already occurred?
315. A browser stores a large serialized state object synchronously on every keystroke; how would you redesign persistence to reduce interaction delay?
316. A TypeScript type assertion hides an unexpected API shape; how would you replace it with a runtime boundary check?
317. A discriminated union gains a new variant; how would you force every relevant switch statement to handle it?
318. A TypeScript function accepts an object with optional fields; how would you distinguish omission from a field explicitly assigned undefined?
319. A generic TypeScript function returns the same type parameter it accepts but constructs a different object; what unsound promise is it making?
320. A callback parameter type is narrowed too far when implementing a generic consumer; how would you explain the substitutability problem?
321. A TypeScript library publishes declarations that reference a private dependency type; how can that break consumers who never installed the dependency?
322. An index signature says every string key has a value, but runtime lookups can miss; how would you make absence visible to the type checker?
323. A TypeScript enum's runtime representation is serialized into a public API; what compatibility risks arise when members are reordered?
324. A mapped type makes top-level properties readonly; which nested mutations remain possible and how should the API document them?
325. A function receives unknown caught errors; how would you safely extract a message without assuming every thrown value is an Error?
326. A TypeScript path alias works in the editor but fails in deployed JavaScript; which build and runtime resolution boundaries would you inspect?
327. A package exposes both CommonJS and ES modules; how would you test whether both entry points preserve the same public behavior?
328. A circular ES module dependency accesses a binding before initialization; how would you refactor the initialization sequence?
329. A module has import-time side effects that prevent tree shaking; how would you separate registration from reusable definitions?
330. A TypeScript declaration compiles against newer DOM types than the supported browser implements; how would you distinguish type availability from runtime support?
331. An order service commits an order but cannot publish its event; how would you make eventual publication recoverable without holding the database transaction open?
332. An outbox dispatcher publishes an event and crashes before marking it sent; how should downstream consumers handle the replay?
333. An event consumer updates a projection before acknowledging the message; how would it recognize a redelivery after restart?
334. A customer changes their address while an order is being shipped; which service should decide whether the shipment uses the old or new address?
335. A saga compensates a payment after inventory reservation fails; what should happen if the refund operation also fails?
336. A service accepts a command that will complete asynchronously; what status model distinguishes accepted, running, succeeded, failed, and compensated outcomes?
337. A distributed lock expires during a long operation; what prevents the previous holder from writing after another holder takes over?
338. Two regions both accept updates to one business entity during a network partition; how would you choose and explain the conflict-resolution rule?
339. A service's local clock jumps backward; which ordering, expiry, and identifier assumptions could be invalidated?
340. Events from one producer arrive in different orders at two consumers; what ordering guarantees does the business actually require?
341. A projection receives version 12 before version 11; how would you decide between buffering, rejection, and snapshot recovery?
342. An event schema renames a field consumed by older services; how would you evolve the contract without coordinated deployment?
343. A replay must rebuild a projection without sending customer notifications again; how would you separate projection work from external side effects?
344. A new consumer starts from the beginning of a years-old event stream; what data-retention and schema-compatibility assumptions must hold?
345. A message contains only an entity ID, and the entity is deleted before consumption; how would you decide whether the event needs a snapshot?
346. A command is retried after the client loses the response; how long must the server retain its deduplication record?
347. A cache stores a negative lookup while the underlying entity is created; how would you define acceptable staleness and invalidation?
348. An invalidation message arrives before the corresponding database transaction becomes visible; how could a reader repopulate stale cache data?
349. Two cache layers have different expiration policies; how would you reason about the maximum staleness visible to users?
350. A cache entry is shared across authorization contexts; what information belongs in the key versus a separate permission check?
351. A circuit breaker opens for all callers because one tenant overloads a dependency; how would you decide the breaker's scope?
352. A retry budget is independently enforced by three service layers; how would you calculate and constrain the total request amplification?
353. A fallback returns stale data after a dependency outage; how should consumers learn its age and limitations?
354. A bulkhead reserves worker slots but shares one database connection pool; where can failure isolation still collapse?
355. A request deadline is passed through several services; how would each service determine how much work and retry time remains?
356. An asynchronous workflow spans hours while a caller's HTTP deadline is seconds; where should durable workflow ownership begin?
357. A deployment changes the hashing algorithm used for partition routing; how would you migrate existing records without sending reads to the wrong partition?
358. One large tenant exceeds a partition's capacity; how would you split its workload while preserving tenant-level queries?
359. A globally unique identifier embeds time and node information; which failure cases must its generator handle after restarts or clock changes?
360. A service discovers peers through a stale registry; how would it recover from repeatedly selecting an endpoint that no longer exists?
361. A load balancer sends equal request counts to unequal-capacity workers; what evidence should inform a more suitable routing policy?
362. A job scheduler grants a task to a worker that stops sending heartbeats; how should reassignment handle a worker that is slow rather than dead?
363. A file-processing pipeline produces an output before committing its checkpoint; how would you prevent duplicate outputs on restart?
364. A distributed counter must support billing reconciliation; when are approximate or eventually consistent increments insufficient?
365. A rate limiter must operate across regions during a partition; what availability-versus-global-limit tradeoff must be chosen?
366. A notification service sends both email and SMS; how would you model partial completion when only one channel succeeds?
367. A webhook receiver needs to verify signatures over the original request bytes; which middleware transformations can break verification?
368. A webhook sender retries for several days; how would the receiver handle messages whose referenced state has since changed?
369. A service migrates from synchronous calls to events; how would users observe completion and errors that were previously returned directly?
370. A read model is temporarily inconsistent with the command model; how would the interface avoid presenting a successful write as missing?
371. A shared database table becomes owned by one service; how would you remove other services' direct writes without an abrupt cutover?
372. A monolith is split along technical layers rather than business capabilities; what runtime coupling would indicate the boundaries are wrong?
373. A feature crosses several bounded contexts with different definitions of customer; how would you map identifiers and ownership at the boundary?
374. A configuration service is unavailable during startup; which settings can safely use defaults and which should prevent readiness?
375. A feature flag changes the meaning of stored data; how would rollback work after some users have written the new representation?
376. A reconciliation job finds a mismatch between payment and order state; what evidence is required before applying an automated repair?
377. A recovery procedure replays commands instead of immutable events; how might current business rules change the reconstructed outcome?
378. A distributed trace is incomplete because one service was sampled out; how would you investigate causality without treating missing spans as missing work?
379. A system reports high availability but silently loses accepted jobs; how would you redefine the success metric around the promised outcome?
380. A disaster-recovery exercise restores databases but omits queued work and encryption keys; what additional state belongs in the recovery inventory?
381. An endpoint validates a user's role but not the requested object's tenant; how would you expose this authorization gap with a focused test?
382. A user can submit an ownerId field during object creation; how should the server distinguish client-editable fields from authority-controlled fields?
383. A file download uses a path assembled from request input; how would you confine resolution to the intended storage directory?
384. A URL-fetching feature accepts redirects; how would you prevent an allowed public URL from redirecting to an internal service?
385. An image upload passes its extension check but contains different content; what validation and processing boundaries should apply?
386. A ZIP import contains paths escaping its destination directory; how would you inspect entries before extraction?
387. A compressed request expands to an enormous body; where would you enforce limits on compressed size, expanded size, and processing time?
388. A password-reset response reveals whether an account exists through timing; how would you investigate and reduce that disclosure?
389. A reset token is single-use, but two redemption requests arrive concurrently; how would you enforce one successful redemption atomically?
390. An email-change workflow sends verification only to the new address; how would you evaluate account-takeover and recovery requirements?
391. A logout endpoint removes the browser cookie but leaves refresh tokens usable; what session termination guarantees does it actually provide?
392. A refresh token is rotated, then the previous token appears again; how would you distinguish retry behavior from possible token theft?
393. A JWT contains an unrecognized key identifier; how should key retrieval behave without allowing arbitrary network destinations?
394. An API validates token signatures but ignores audience; what cross-service token misuse could become possible?
395. A token carries permission claims that outlive an administrator's role change; how would you choose between immediate checks and bounded staleness?
396. A mobile client stores a client secret distributed with every installation; what trust assumption should be reconsidered?
397. An OAuth callback accepts an arbitrary return URL; how would you constrain navigation without breaking legitimate deep links?
398. Two login attempts run in different tabs; how would you bind each callback to the correct initiation state?
399. A session cookie is sent automatically with a state-changing request; what determines whether the endpoint needs CSRF protection?
400. A CORS policy reflects any Origin while allowing credentials; why should the application not treat that policy as an authorization boundary?
401. A frontend hides an administrative action but its API remains callable; what server-side policy should the API enforce?
402. A support tool impersonates customers; how would you constrain its permissions and record actions without exposing reusable customer credentials?
403. A privileged background worker processes jobs submitted by ordinary users; how would it revalidate the authority needed for each action?
404. A tenant administrator can invite users by email; how would you prevent the invitation flow from granting access to an unintended tenant?
405. An API rate limit is keyed only by IP behind a shared proxy; how would you balance abuse prevention with legitimate shared-network users?
406. An attacker sends many unique cache keys to a public endpoint; how would you prevent cache memory exhaustion?
407. A search endpoint accepts a regular expression; how would you limit computational abuse without assuming every valid expression is cheap?
408. A templating engine renders user-supplied expressions; what separates safe text interpolation from server-side template execution?
409. A Markdown renderer allows raw HTML; what sanitization boundary must hold before the result reaches the browser?
410. A content-security policy uses a static nonce for every request; why does that fail to provide the intended per-response trust boundary?
411. A page embeds an untrusted iframe and exchanges messages; how should each side validate sender origin and message shape?
412. A log entry includes raw user input with newlines; how would you prevent forged log records and preserve forensic usefulness?
413. A monitoring agent captures full HTTP headers; how would you identify and redact credentials before export?
414. A secret was accidentally committed and then removed from the latest file; what further steps are needed because history and copies may retain it?
415. An encryption key is rotated while old records remain encrypted; how would you identify keys per record and plan re-encryption?
416. An encrypted database field is searchable through a deterministic token; what information can that token still reveal?
417. A backup contains encrypted records but its key material is stored beside it; how would you evaluate the actual protection against backup theft?
418. A digital signature verifies file content but not its expected purpose; how would you prevent a signed artifact from being accepted in the wrong context?
419. A webhook signature includes a timestamp; how would you prevent replay while allowing reasonable clock differences?
420. A payment request's amount is signed but its currency is not; what substitution risk should the signature contract address?
421. A package manager resolves an internal package name from a public registry; how would you prevent dependency-confusion installation?
422. A CI job runs code from an untrusted pull request; which secrets and write permissions should be unavailable to that job?
423. A build artifact is scanned before signing, then modified during packaging; how would you preserve the link between reviewed and deployed bytes?
424. A container runs as a non-root user but mounts a powerful host socket; what remaining authority must be evaluated?
425. A service account has wildcard permissions to simplify deployment; how would you discover the permissions it actually exercises?
426. A database connection uses a shared administrator identity; how would you migrate workloads toward separate least-privilege identities?
427. A security audit requires evidence of access changes; what makes an audit log resistant to modification by the same actors it records?
428. A user requests deletion while backups and event logs retain their data; how would you map the technical retention paths before promising an outcome?
429. A model-generated tool call requests a privileged operation; why must the application validate authorization independently of the model's output?
430. A retrieved document tells an AI agent to export secrets; how should the system preserve the distinction between document content and trusted instructions?
431. A test suite passes in isolation but fails when run in random order; how would you identify state shared between tests?
432. A test fixture creates a database schema once for parallel tests; how would you prevent records from one test changing another test's result?
433. A test verifies that an email method was called but not its recipient; what business failure can still pass unnoticed?
434. A mocked repository returns insertion order, but production SQL specifies no ordering; how would you expose that assumption?
435. A test uses an in-memory database for transaction behavior; which real-engine characteristics should be covered by integration tests?
436. A contract test checks JSON property names but ignores nullability; how would you test a change that makes an optional field mandatory?
437. A consumer contract allows extra response fields; how would you distinguish compatible additions from changes to existing field meaning?
438. A provider passes all current contract tests but an older consumer is still deployed; what version coverage is missing?
439. A UI test clicks a button before it is enabled and occasionally fails; how would you wait for the user-visible condition instead of adding a fixed sleep?
440. A browser test locates elements by generated CSS classes; what selector contract would survive harmless visual refactoring?
441. A visual regression test flags timestamps and random IDs on every run; how would you control nondeterminism without hiding meaningful changes?
442. A screenshot test runs with a different font fallback in CI; how would you verify rendering dependencies before accepting new baselines?
443. A keyboard-only test cannot reach a dialog's close button; how would you distinguish focus-order, visibility, and event-handling failures?
444. An API test follows redirects automatically and reports success; how could that hide an authentication failure?
445. A retry test supplies two failures and a success; how would you also verify that the operation stops retrying after its deadline?
446. A cancellation test completes before cancellation is requested; how would you ensure it exercises the intended in-flight state?
447. A concurrency test rarely reproduces a lost update; how would you coordinate execution around the critical read and write points?
448. A deadlock fix passes under a single thread; what targeted concurrent test would exercise opposite lock-acquisition paths?
449. A queue consumer test acknowledges messages automatically; how would you simulate a crash after side effects but before acknowledgement?
450. A message handler is tested only once per message; which assertions would establish idempotency under redelivery?
451. A property-based test finds a failing input that is hard to interpret; how would shrinking help isolate the broken invariant?
452. A sorting function returns the expected sample output; what properties would detect lost, duplicated, or incorrectly ordered elements more broadly?
453. A serializer and deserializer round-trip their own output; why might they still be incompatible with an external implementation?
454. A numerical algorithm is tested against the same formula used in its implementation; how would you obtain an independent reference result?
455. A mutation-testing tool changes a comparison operator without causing failures; what gap in the tests might that reveal?
456. Coverage rises after tests execute code without checking outcomes; how would you assess whether confidence actually improved?
457. A load test reports response latency only for successful requests; how would you account for timeouts and rejected work?
458. A stress test overwhelms the load generator before the application; which measurements would prevent a false server-capacity conclusion?
459. A soak test shows stable request latency but steadily increasing handles; what additional acceptance criterion should fail the test?
460. A chaos experiment disconnects a dependency; how would you define safe scope, abort conditions, and expected recovery before running it?
461. A failover test succeeds with no traffic but fails during active writes; what consistency and recovery checks belong in the loaded test?
462. A backup test checks that a file exists; what restore validation would prove the backup is usable?
463. A migration test compares row counts only; how would you add checks for constraints, precision changes, and referential meaning?
464. A rollback test reverts application code but leaves a new schema in place; what compatibility paths must still work?
465. A canary deployment has too little traffic to reveal a regression; how would you decide whether its observation window is sufficient?
466. A feature-flag test suite covers each flag separately; how would you prioritize risky combinations without testing every combination?
467. A time-dependent test crosses a daylight-saving boundary; what clock and timezone fixtures make the expected behavior reproducible?
468. A monthly billing test always uses dates in the middle of the month; which boundary dates and calendar lengths should be added?
469. A parser fuzz test crashes on malformed Unicode; how would you preserve the failing input and distinguish invalid input handling from a security issue?
470. A security regression test succeeds only with an administrator token; what lower-privilege and cross-tenant cases are missing?
471. A test mock verifies exact call order even though independent operations may run concurrently; how would you assert only the required ordering constraints?
472. A test retries itself until it passes; how would you distinguish environmental instability from a product race instead of masking both?
473. A flaky-test quarantine removes a critical payment test from release gates; what temporary coverage and ownership should accompany that decision?
474. A test data factory produces only valid objects; how would you systematically exercise invalid boundary states?
475. A production incident involves a payload far larger than test fixtures; how would you build representative size distributions for future tests?
476. A monitoring rule is changed without any alert test; how would you verify both detection and the absence of alerts on normal traffic?
477. A test of structured logging asserts a formatted string; how would you verify stable event fields and redaction instead?
478. A test container starts accepting connections before its schema is ready; what readiness condition should the test harness await?
479. A CI pipeline caches build outputs across dependency changes; how would you prove that tests ran against the intended source and dependencies?
480. A release passes automated tests but support cannot perform recovery; how would you make operational rehearsal part of release evidence?
481. A RAG system retrieves a relevant document whose access permissions changed after indexing; where must authorization be rechecked?
482. Two documents provide conflicting instructions with different effective dates; how should retrieval and answer generation expose that conflict?
483. A document update changes one paragraph but the embedding index keeps older chunks; how would you prevent mixed-version answers?
484. A chunk boundary separates a requirement from its exception; how would you evaluate and correct the resulting retrieval failure?
485. An evaluation set contains questions generated from the same documents used to tune retrieval; what bias could overstate real-user performance?
486. A search query contains an exact product code and a natural-language request; how would you evaluate keyword and vector retrieval contributions?
487. An answer cites a document that mentions the topic but does not support the claim; how would you test citation entailment separately from retrieval relevance?
488. A model returns valid JSON with an invalid business identifier; what validation must occur after schema parsing?
489. An agent proposes deleting a resource while the user asked only for a status check; what application boundary should reject the proposed action?
490. A tool call returns an uncertain timeout after creating an external record; how would the agent reconcile state before retrying?
491. An agent loop keeps alternating between two tools without progress; what observable stopping rule would bound cost and execution time?
492. A conversation summary drops a user's earlier restriction; how would you preserve constraints separately from ordinary summarized dialogue?
493. A semantic cache matches two similar questions from different tenants; which scope and data-version checks must run before reusing the answer?
494. A model upgrade improves average quality but worsens refusal or tool-selection behavior; how would you evaluate those outcomes independently?
495. An evaluation judge prefers longer answers regardless of correctness; how would you detect and reduce that judging bias?
496. A retrieval experiment improves recall but adds irrelevant context that hurts answers; how would you measure the end-to-end tradeoff?
497. A tool result contains a hidden instruction to change the workflow; how should the agent treat the result's data and instructional text?
498. A streaming model response fails after the client has displayed half the answer; what completion and retry contract should the interface expose?
499. A system must remove a user's documents from retrieval, caches, and stored conversation context; how would you track completion across those stores?
500. An AI feature is described as accurate after passing a small demonstration; what evaluation evidence would you require before making a production accuracy claim?
